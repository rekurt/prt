<div align="center">

# prt

**Мониторинг сетевых портов в реальном времени прямо в терминале**

<br>

<img src="docs/prt.gif" alt="prt демо" width="720">

<br>
<br>

[![Crates.io](https://img.shields.io/crates/v/prt.svg)](https://crates.io/crates/prt)
[![Downloads](https://img.shields.io/crates/d/prt.svg)](https://crates.io/crates/prt)
[![CI](https://github.com/rekurt/prt/actions/workflows/ci.yml/badge.svg)](https://github.com/rekurt/prt/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Rust](https://img.shields.io/badge/rust-1.75%2B-orange.svg)](https://www.rust-lang.org)
[![docs.rs](https://docs.rs/prt-core/badge.svg)](https://docs.rs/prt-core)

[English](README.md) | [Русский](README.ru.md) | [中文](README.zh.md)

</div>

---

## Что такое prt?

`prt` показывает, какие процессы занимают сетевые порты — в реальном времени, прямо в терминале. Интерактивный аналог `lsof -i` / `ss -tlnp` с подсветкой, фильтрацией и деревом процессов.

## Возможности

### Живая таблица с отслеживанием изменений

Главное представление — сортируемая, фильтруемая таблица всех активных сетевых соединений. Колонки: порт, сервис, протокол, состояние, PID, процесс, пользователь. Новые соединения подсвечиваются **зелёным**, закрытые — **красным** на 5 секунд. Обновление каждые 2 секунды.

### База известных портов

Колонка `Service` сопоставляет порты с читаемыми именами — http (80), ssh (22), postgres (5432) и ~170 других. Можно дополнить или переопределить в `~/.config/prt/config.toml`:

```toml
[known_ports]
3000 = "my-app"
9090 = "prometheus"
```

### Возраст соединений

Каждое соединение отслеживает время создания (`first_seen`). ESTABLISHED старше 1 часа подсвечиваются жёлтым, старше 24 часов — красным. CLOSE_WAIT всегда красные, так как указывают на возможную утечку ресурсов.

### Детектор подозрительных соединений

Соединения проверяются на аномалии и помечаются `[!]`:

- **Non-root на привилегированном порту** — не-root процесс слушает порт < 1024
- **Скрипт на чувствительном порту** — Python, Perl, Ruby или Node.js слушают порт 22, 80 или 443
- **Root с исходящим на высокий порт** — root-процесс с established-соединением на удалённый порт > 1024

Фильтр: нажмите `/`, затем `!` — покажутся только подозрительные.

### Контейнеры Docker/Podman

Если запущен Docker или Podman, колонка `Container` показывает имя контейнера для каждого процесса. Колонка автоматически скрывается, если контейнеров нет. Разрешение выполняется пакетно через `docker ps` + `docker inspect` с таймаутом 2 секунды.

### Оценка пропускной способности

В заголовке отображается скорость сетевого трафика: `▼ 1.2 MB/s ▲ 340 KB/s`. На Linux читается `/proc/net/dev`, на macOS — `netstat -ib`. Скорости рассчитываются как дельты между циклами обновления.

### Дерево процессов

Нажмите `Enter` или `d` для открытия панели деталей, затем `1` — отобразится цепочка родительских процессов (например, `launchd → nginx → worker`).

### Вкладки панели деталей

Нижняя панель (переключение — `Enter`/`d`) содержит три вкладки:

| Вкладка | Клавиша | Содержание |
|---------|---------|------------|
| **Дерево** | `1` | Цепочка родительских процессов |
| **Сеть** | `2` | Сетевые интерфейсы, IP-адреса, MTU |
| **Соединения** | `3` | Все соединения выбранного PID |

### Полноэкранные режимы

Четыре специализированных вида по клавишам `4`-`7`:

| Режим | Клавиша | Описание |
|-------|---------|----------|
| **График** | `4` | Горизонтальная гистограмма — количество соединений по процессам |
| **Топология** | `5` | ASCII-граф сети: процесс → локальный порт → удалённый хост |
| **Детали процесса** | `6` | Полная информация: CWD, CPU %, RSS, открытые файлы, переменные окружения, все соединения, сетевые интерфейсы, дерево процессов |
| **Namespaces** | `7` | Группировка по сетевым пространствам имён (только Linux). Показывает имена из `/run/netns/` или инод-номера |

Во всех режимах работает скролл (`j`/`k`, `g`/`G`). `Esc` — возврат к таблице.

### Блокировка IP через файрвол

Нажмите `b` на соединении с удалённым адресом для блокировки IP. Диалог подтверждения показывает точную команду:

- **Linux:** `iptables -A INPUT -s <IP> -j DROP`
- **macOS:** `pfctl -t prt_blocked -T add <IP>`

После блокировки в статусной строке отображается команда отмены. Требует sudo.

### Strace / Dtruss

Нажмите `t` для подключения трассировщика системных вызовов к выбранному процессу. Панель деталей разделяется для показа потока сетевых syscall:

- **Linux:** `strace -p <PID> -e trace=network -f`
- **macOS:** `dtruss -p <PID>` (требует отключённый SIP или root)

Повторное нажатие `t` отключает трассировку. Процесс трассировщика автоматически завершается при выходе.

### SSH-проброс портов

Нажмите `F` (Shift+F) для создания SSH-туннеля для выбранного порта. Диалог запрашивает удалённый хост:

```
localhost:5432 →
хост:порт → user@server.io:5432█
```

Туннель создаётся через `ssh -N -L <local>:localhost:<remote> <host>`. Активные туннели отображаются в заголовке (`⇄ localhost:5432 → server:22`). Туннели проверяются каждый тик и автоматически завершаются при выходе через `Drop`.

### Правила алертов

Определите правила в `~/.config/prt/config.toml`:

```toml
[[alerts]]
port = 22
action = "bell"        # звуковой сигнал при новых SSH-соединениях

[[alerts]]
process = "python"
state = "LISTEN"
action = "highlight"   # подсветка строки жёлтым

[[alerts]]
connections_gt = 100
action = "bell"        # сигнал когда процесс превышает 100 соединений
```

Алерты срабатывают только на НОВЫХ записях (не каждый цикл обновления). Условия: `port`, `process`, `state`, `connections_gt`. Действия: `bell`, `highlight`.

### NDJSON-потоком

```bash
prt --json | jq '.process.name'
```

Выводит один JSON-объект на соединение за цикл обновления. Корректно обрабатывает SIGPIPE (без паник при пайпе в `head`). Без TUI — безопасен для скриптов.

### Watch-режим

```bash
prt watch 3000 8080 5432
```

Компактный не-TUI дисплей с UP/DOWN-статусом для конкретных портов. Звуковой сигнал (BEL) при смене состояния. ANSI-цвета при выводе в терминал, чистый текст при пайпе.

```
:3000 ● UP   nginx (1234)   since 14:32:05
:8080 ○ DOWN                 since 14:35:12
:5432 ● UP   postgres (567)  since 14:32:05
```

### Экспорт

```bash
prt --export json    # JSON-снимок всех соединений
prt --export csv     # CSV-снимок
```

### Мультиязычный интерфейс

Английский, русский, китайский. Приоритет определения:

1. Флаг `--lang en|ru|zh` (высший приоритет)
2. Переменная окружения `PRT_LANG`
3. Автоопределение по системной локали
4. Английский (по умолчанию)

Клавиша `L` переключает язык без перезапуска.

## Установка

```bash
cargo install prt
```

<details>
<summary><b>Сборка из исходников</b></summary>

```bash
git clone https://github.com/rekurt/prt.git
cd prt
make install    # или: cargo install --path crates/prt
```

**Требования:** Rust 1.75+ · macOS 10.15+ или Linux с `/proc` · `lsof` (macOS — предустановлен)

</details>

## Использование

```bash
prt                     # запустить TUI
prt --lang ru           # русский интерфейс
prt --export json       # экспорт в JSON
prt --export csv        # экспорт в CSV
prt --json              # NDJSON потоком в stdout
prt watch 80 443 5432   # компактный мониторинг портов
sudo prt                # запуск от root (все процессы)
```

## Горячие клавиши

**Навигация:**

| Клавиша | Действие |
|---------|----------|
| `j`/`k` `↑`/`↓` | Перемещение / скролл |
| `g` / `G` | В начало / конец |
| `/` | Поиск и фильтр (`!` = подозрительные) |
| `Esc` | Назад к таблице / сбросить фильтр |
| `q` | Выход |

**Нижняя панель (режим таблицы):**

| Клавиша | Действие |
|---------|----------|
| `Enter` / `d` | Показать/скрыть панель деталей |
| `1` `2` `3` | Дерево / Сеть / Соединение |
| `←`/`→` `h`/`l` | Переключение вкладок |

**Полноэкранные режимы:**

| Клавиша | Действие |
|---------|----------|
| `4` | График — соединения по процессам |
| `5` | Топология — процесс → порт → удалённый |
| `6` | Детали процесса — инфо, файлы, env |
| `7` | Namespaces (только Linux) |

**Действия:**

| Клавиша | Действие |
|---------|----------|
| `K` / `Del` | Завершить процесс |
| `c` | Копировать строку |
| `p` | Копировать PID |
| `b` | Блокировать IP (firewall) |
| `t` | Подключить/отключить strace |
| `F` | SSH-проброс порта (туннель) |
| `r` | Обновить |
| `s` | Sudo пароль |
| `Tab` | След. колонка сортировки |
| `Shift+Tab` | Направление сортировки |
| `L` | Переключить язык |
| `?` | Справка |

## Конфигурация

Создайте `~/.config/prt/config.toml`:

```toml
# Пользовательские имена портов
[known_ports]
3000 = "my-app"
9090 = "prometheus"

# Правила алертов
[[alerts]]
port = 22
action = "bell"

[[alerts]]
process = "python"
state = "LISTEN"
action = "highlight"

[[alerts]]
connections_gt = 100
action = "bell"
```

## Разработка

```bash
cargo build --workspace          # сборка
cargo test --workspace           # тесты (188 тестов)
cargo clippy --workspace         # линтер
cargo fmt --all -- --check       # проверка форматирования
cargo bench -p prt-core          # бенчмарки
```

Подробнее в [CONTRIBUTING.md](CONTRIBUTING.md).

## Лицензия

[MIT](LICENSE)
